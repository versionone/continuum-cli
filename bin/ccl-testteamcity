#!/usr/bin/env python
import sys
import os
sys.path.insert(0, os.path.join(os.environ["CCL_HOME"], "flow", "plugins"))
from teamcity import job
if len(sys.argv) > 1:
    instance = sys.argv[1]
else:
    instance = None
job.connect(instance)
