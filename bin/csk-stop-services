#!/usr/bin/env python

#########################################################################
# Copyright 2015 ClearCode Labs
# All Rights Reserved.
# http://www.clearcodelabs.com/license.html
#########################################################################


# This command does not have an API counterpart.  
# It calls the scripts to stop services.
# it's done in python so it can handle --dumpdoc for the 
#    automatic documentation generator.

import os
import glob
import sys

if __name__ == '__main__':
    if len(sys.argv) > 1:
        # if there's an argument, simply switch on it.  It'll only be asking for the api or dumpdoc.
        if sys.argv[1] == "--dumpdoc" or sys.argv[1] == "-H":
            print """Stop ALL ClearCode services.  Accepts no arguments."""
            exit()
        else:
            exit()
            
    os.system("$CSK_HOME/common/bin/stop_services.sh")
