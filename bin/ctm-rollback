#!/usr/bin/env python

import os
import sys
if "CCL_HOME" in os.environ:
    sys.path.append(os.path.join(os.environ["CCL_HOME"], "common", "lib"))
import ctmcommands.admin.rollback

if __name__ == '__main__':
    cmd = ctmcommands.admin.rollback.Rollback()
    cmd.main()
