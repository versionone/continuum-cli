#!/usr/bin/env python

import os
import sys
if "CCL_HOME" in os.environ:
    sys.path.append(os.path.join(os.environ["CCL_HOME"], "common", "lib"))
import cskcommands.admin.update

if __name__ == '__main__':
    cmd = cskcommands.admin.update.Update()
    cmd.main()
