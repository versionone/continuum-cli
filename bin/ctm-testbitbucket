#!/usr/bin/env python

import os
import sys
sys.path.insert(0, os.path.join(os.environ['CONTINUUM_HOME'], 'common', 'lib'))
import ctmcommands.flow.testbitbucket

if __name__ == '__main__':
    cmd = ctmcommands.flow.testbitbucket.TestBitBucket()
    cmd.main()
