#!/usr/bin/env python

#########################################################################
# Copyright 2016 VersionOne
# All Rights Reserved.
# http://www.versionone.com
#########################################################################


# This command does not have an API counterpart.  
# It calls the scripts to restart services.
# it's done in python so it can handle --dumpdoc for the 
#    automatic documentation generator.

import os
import glob
import sys

if __name__ == '__main__':
    if len(sys.argv) > 1:
        # if there's an argument, simply switch on it.  It'll only be asking for the api or dumpdoc.
        if sys.argv[1] == "--api" or sys.argv[1] == "--dumpdoc" or sys.argv[1] == "-H":
            print """Restart one or more Continuum services, specified as space delimited arguments."""
            exit()
        else:
            svcs = " ".join(sys.argv[1:])
            os.system("$CONTINUUM_HOME/common/bin/restart_specific.sh %s" % (svcs))
            exit()
    else:
        print("Please specify at least one service name.")
